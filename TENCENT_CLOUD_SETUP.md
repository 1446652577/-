# 腾讯云真实文档解析配置指南

## 一、总体流程

```
用户上传文件 → 云存储 → 云函数下载 → 按格式解析文本 → 腾讯云OCR(图片类) → 混元大模型生成题目 → 返回JSON
```

| 文件格式 | 解析方式 |
|---------|---------|
| **.txt** | 直接读取文本 |
| **.docx** | mammoth库提取 |
| **.pdf** | pdf-parse提取 + OCR兜底 |
| **.ppt/.pptx/.doc/.xls/.xlsx** | 腾讯云OCR识别 |
| **图片类** | 腾讯云OCR识别 |

---

## 二、需要申请的腾讯云资源

### 1. 访问密钥（SecretId + SecretKey）

这是调用所有腾讯云API的凭证。

**申请路径**：
1. 登录 [腾讯云控制台](https://console.cloud.tencent.com/)
2. 右上角头像 → **访问管理**
3. 左侧菜单 → **访问密钥** → **API密钥管理**
4. 点击 **「新建密钥」**
5. 复制 **SecretId** 和 **SecretKey**（⚠️ SecretKey只显示一次，务必保存）

### 2. 腾讯云OCR文字识别（你已开通 ✓）

你截图中显示已开通，每月有 **1000次免费额度**。

**确认开通路径**：
1. [腾讯云OCR控制台](https://console.cloud.tencent.com/ocr/overview)
2. 查看「通用印刷体识别」是否显示「已开通」
3. 如未开通，点击对应接口的「免费开通」即可

**计费说明**：
- 通用印刷体识别（GeneralBasicOCR）：**1000次/月免费**
- 通用印刷体识别（高精度版）：1000次/月免费
- 超出后约 ¥0.015/次

### 3. 腾讯云混元大模型

**申请路径**：
1. 登录 [腾讯云混元大模型](https://console.cloud.tencent.com/hunyuan)
2. 点击「立即开通」
3. 开通后进入「API密钥管理」，获取 API Key

**计费说明**：
- hunyuan-lite（轻量版）：**有免费额度**，适合轻量应用
- hunyuan-pro（专业版）：按token计费
- 建议先用 **hunyuan-lite** 测试

---

## 三、配置云函数环境变量

⚠️ **切勿将SecretId/SecretKey硬编码在代码中！** 必须使用环境变量。

### 设置方法：

1. 打开 **微信开发者工具**
2. 点击上方工具栏的 **「云开发」** 按钮
3. 进入 **「云函数」** 标签页
4. 找到 `parseDocument` 云函数
5. 点击 **「版本与配置」**
6. 选择 **「环境变量」** 标签
7. 添加以下变量：

| 变量名 | 值 | 说明 |
|-------|---|------|
| `TENCENT_SECRET_ID` | 你的SecretId | 腾讯云API密钥ID |
| `TENCENT_SECRET_KEY` | 你的SecretKey | 腾讯云API密钥 |

8. 点击 **「保存」**
9. **重新部署** `parseDocument` 云函数（右键 → 创建并部署：云端安装依赖）

---

## 四、云函数部署步骤

### 第1步：安装依赖

在微信开发者工具中：

1. 右键 `cloudfunctions/parseDocument` 文件夹
2. 选择 **「打开终端」**
3. 运行：
```bash
npm install
```

或者直接用云端安装依赖：
1. 右键 `cloudfunctions/parseDocument`
2. 选择 **「创建并部署：云端安装依赖」**

### 第2步：配置环境变量

按上面的「三、配置云函数环境变量」操作。

### 第3步：调整超时时间

文档解析可能耗时较长，需要增加云函数超时时间：

1. 云开发控制台 → 云函数 → `parseDocument`
2. 点击 **「配置」**
3. 将「超时时间」改为 **60秒**（默认3秒不够用）
4. 将「内存」改为 **512MB**

### 第4步：重新部署

右键 `cloudfunctions/parseDocument` → **「创建并部署：云端安装依赖」**

---

## 五、测试验证

部署完成后，测试导入一个文件，查看云函数日志：

1. 微信开发者工具 → 云开发 → 云函数 → 日志
2. 找到 `parseDocument` 的执行记录
3. 查看是否有以下输出：
   - `提取的文本长度: xxx`
   - AI返回的题目JSON

如果OCR或AI调用失败，日志中会显示具体错误信息。

---

## 六、常见问题

### Q1: OCR识别返回空文字？
- 检查文件是否真的是图片/扫描件
- 检查SecretId/SecretKey是否正确
- 检查OCR接口是否已开通

### Q2: 混元大模型调用失败？
- 确认已开通混元大模型服务
- 检查API Key是否正确配置
- 可能免费额度已用完，需要充值

### Q3: PPT解析效果不好？
- PPT本质是图片+文字，OCR只能识别文字部分
- 建议上传前将PPT导出为PDF，文字层更完整
- 或者使用纯文本格式的学习资料

### Q4: 想换成其他AI模型？
可以修改 `parseDocument/index.js` 中的 `generateQuestionsByAI` 函数：
- 文心一言：调用百度API
- 通义千问：调用阿里云API
- ChatGPT：调用OpenAI API
- 智谱AI：调用智谱API

只需替换对应的SDK调用代码，Prompt保持不变。

---

## 七、费用预估（月度）

| 项目 | 免费额度 | 超出费用 | 备注 |
|------|---------|---------|------|
| 腾讯云OCR | 1000次/月 | ~¥0.015/次 | 一般够用 |
| 混元lite | 有免费额度 | 按token | 轻量使用免费 |
| 云函数调用 | 100万次/月 | 极低 | 基本免费 |
| 云存储 | 5GB | ¥0.15/GB/月 | 文件临时存储 |

**正常使用情况下，月度费用几乎为0。**
